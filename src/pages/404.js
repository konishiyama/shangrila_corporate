import React from "react"

const NotFoundPage = () => (
  <section>
    <h1>NOT FOUND</h1>
    <p>You just hit a route that does't exist... the sadness.</p>
  </section>
)

export default NotFoundPage
